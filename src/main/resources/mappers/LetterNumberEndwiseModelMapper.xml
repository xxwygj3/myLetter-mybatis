<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ixygj.myletter.mapper.LetterNumberEndwiseModelMapper">
    <insert id="insertLetterNumberEndwiseModel" parameterType="com.ixygj.myletter.entity.LetterNumberEndwiseModel">
        insert into letternumberendwisemodel(
        endwise_model,model_times
        )
        values (
        #{endwiseModel,jdbcType=VARCHAR},
        #{modelTimes,jdbcType=INTEGER}
        )
    </insert>
</mapper>